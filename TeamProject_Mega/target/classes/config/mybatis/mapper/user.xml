<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="u">

	<!-- 회원가입 -->
	<insert id = "user_insert" parameterType="user">
		insert into USER_INFO values(
				SEQ_USER_INFO.nextVal,
				#{user_id},
				#{user_pwd},
				sysdate,
				#{user_nickname},
				'test',
				#{user_email},
				#{user_name},
				#{user_ip}
		)
	</insert>
	
	<!-- 로그인 -->
	<select id = "user_login" resultType="user" parameterType="user">
		select * from USER_INFO where user_id = #{user_id} and user_pwd = #{user_pwd}
	</select>
	
	<!-- 회원 탈퇴 -->
	<delete id = "delete_id" parameterType="int">
		delete from USER_INFO where user_id=#{user_id} and user_pwd=#{user_pwd}
	</delete>
	
	<!-- 아이디 찾기 -->
	<select id = "select_id" resultType="User" parameterType="int">
<<<<<<< HEAD
		select user_id form USER_INFO where name=#{user_name} and <!-- 아이디 찾기 이름 + 주민번호 or 이름 + 이메일 -->
=======
		select user_id form USER_INFO where name=#{name} and <!-- 아이디 찾기 이름 + 주민번호 or 이름 + 이메일 -->
>>>>>>> branch 'main' of https://github.com/JsKim5/TeamProject.git
	</select>
	
	<!-- 비밀번호 찾기 -->
	<select id = "select_pw" resultType="User" parameterType="int">
<<<<<<< HEAD
		select user_pw form USER_INFO where name=#{user_name} and id=#{user_id} <!-- 비밀번호 찾기 이름 + 아이디 + 주민번호 or 이메일 -->
=======
		select user_pw form USER_INFO where name=#{name} and id=#{id} <!-- 비밀번호 찾기 이름 + 아이디 + 주민번호 or 이메일 -->
>>>>>>> branch 'main' of https://github.com/JsKim5/TeamProject.git
	</select>
	
	<!-- 마이페이지 -->
	<select id = "mypage" resultType="user" parameterType="user">
		select * from USER_INFO where user_id=#{user_id}
	</select>
	
	
</mapper>












